<section class="jumbotron_section">
  <div class="jumbotron">
    <h1>Самой большой портал поиска пропавших вещей.</h1>
    <div class="container">
      <div class="row">
        <div class="search col-md-offset-4 col-md-4">
          <% if !params[:q] %>
            <h3>Введите ключевое слово</h3>
          <% end %>
          <%= form_tag search_path, method: 'get' do %>
            <div class="input-group">
              <input type="text" name="q" value="<%= params[:q] %>">
              <span class="input-group-btn">
                <button class="btn btn-default" type="button">Искать</button>
              </span>
            </div>
          <% end %>
        </div>
      </div>
    </div>
    <p>
      <a class="btn btn-danger btn-lg" href="<%= found_articles_path %>" role="button">Я потерял вещь</a>
      <a class="btn btn-success btn-lg" href="<%= lost_articles_path %>" role="button">Я нашел вещь</a>
    </p>
  </div>
  <a class="btn btn-info btn-lg" href="<%= current_user ? new_article_path : new_user_session_path %>" role="button">Добавить объявление</a>
</section>
<aside>
  <p><em>Объявлений в разделе "Найденные" - </em><strong><%= @found_quantity %></strong> </p>
  <p><em>Объявлений в разделе "Потерянные" - </em><strong><%= @lost_quantity %></strong></p>
</aside>




